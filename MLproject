name: garbage-mlflow-tracking

conda_env: conda.yaml

entry_points:
  main:
    # parameters:
    #   config: {type str, default: config.yaml}
    command: |
          python train.py
#--config {config}